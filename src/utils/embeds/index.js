const playEmbeds = require('./play');
const setupEmbeds = require('./setup');
const playerEmbeds = require('./player');
const statusEmbeds = require('./status');

module.exports = { playEmbeds, setupEmbeds, playerEmbeds, statusEmbeds };
